{% extends 'layout.twig' %}

{% block body %}
    {% include "flash.twig" %}
      <h1>{{page.title}}</h1>
      <p>Bonjour {{ connectedUser.prenom }} {{ connectedUser.nom }} </p>
    <br>
    <table class="table table-hover">


        {% for debts in nbrDebts %}
            <tr>
                <td>test</td>
                <td>{{ debts.amount }} </td>
                <td>{{ debts.sender }}</td>
            </tr>
        {% endfor %}



        </tr>
    </table>



{% endblock %}
