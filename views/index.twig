{% extends 'layout.twig' %}

{% block body %}
    {% include "flash.twig" %}
      <h1>{{page.title}}</h1>
      <p>Welcome to {{page.title}}</p>
        <a class="btn btn-info" href="/connection/connect">Se connecter</a>

{% endblock %}
