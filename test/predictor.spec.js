let User = require('../model/user');
//let expect = require('chai').expect;
//let assert = require('chai').assert;
let friendPredictor = require('../model/friendPredictor');
let Mongo = require('mongodb');
let MongoClient = require('mongodb').MongoClient;
let conf = require('../congif/config');
let expect = require('chai').expect;
let assert = require('chai').assert;


describe("friendPredictor", ()=>{

  /*

    it('shloud be able to add a friend to an existing user', function(){


      User.getUserInfo("5afec7ce6ebeb22ac8701394",function(user){
        let res = [];
        console.log("starting prediction");
        friendPredictor.getPredictorCount(user, res);
  });



  });
*/

});
