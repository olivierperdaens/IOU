function  probabilityFinder( friendMatrix  ){
    var baseArray = friendMatrix[0].slice();
    //copies the first array to to base

    for (var i = 0 ; i < friendMatrix[0].length ; i++){
        for( var j =0 ; j < friendMatrix[i].length; j++ ){



        }



    }

}